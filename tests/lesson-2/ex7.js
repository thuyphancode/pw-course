// In ra các giá trị chia hết cho 3 từ 1000 tới 20000
for (let i = 1000; i <= 2000; i++) {
    if (i % 3 == 0) {
        console.log(`${i} chia hết cho 3`);
    } else {
        console.log(`${i} k chia hết cho 3`);
    }
}